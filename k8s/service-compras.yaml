apiVersion: v1
kind: Service
metadata:
  name: compras-service
spec:
  selector:
    app: compras
  ports:
    - protocol: TCP
      port: 80          # porta dentro do cluster
      targetPort: 8080  # porta do container (definida no Deployment)
  type: NodePort
